# 得到提示词
# messages = prompt.format_message(**inputs)
## 得到AI返回的消息
# ai = model.invoke(messages)
## 从AI返回的消息中提取我们需要的文本
# text = ai.content
#
#
## LECL
# chain = prompt | model | StrOutputParser()
# text = chain.invoke(inputs)
